import { CustDirective } from './cust.directive';

describe('CustDirective', () => {
  it('should create an instance', () => {
    const directive = new CustDirective();
    expect(directive).toBeTruthy();
  });
});
